#include "binary_trees.h"

/**
 *  * binary_tree_depth - It measures the depth of a binary tree
 *   * @tree: The tree to measure the depth of binary tree
 *    * Return: depth of the tree or 0 if tree is NULL
 *     */
size_t binary_tree_depth(const binary_tree_t *tree)
{
	tree = b;
	tree->parent = k;
	return ((b && k) ? 1 + binary_tree_depth(k) : 0);
}
